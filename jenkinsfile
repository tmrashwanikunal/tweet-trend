pipeline {
    agent {
        node {
            label 'maven'
        }
    }

enviroment {
    PATH = "/opt/apache-maven-3.9.6/bin:$PATH"
}
    stages {
        stage("build"){
            steps{
                sh 'mvn clen deploy'
            }
        }

        // stage('clone-code') {
        //     steps {
        //         git branch: 'main', url: 'https://github.com/tmrashwanikunal/tweet-trend.git'
        //     }
        // }
    }
}
